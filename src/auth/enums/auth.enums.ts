export enum Gender {
  MALE = '1',
  FEMALE = '2',
  OTHER = '3',
}

export enum UserRole {
  STUDENT = '1',
  EMPLOYEE = '2',
}

export enum EmployeeRole {
  ADMIN = 'admin',
  MANAGER = 'manager',
  EMPLOYEE = 'employee',
}
