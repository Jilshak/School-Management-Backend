export enum Gender {
  MALE = '1',
  FEMALE = '2',
  OTHER = '3',
}

export enum UserRole {
  STUDENT = 'student',
  EMPLOYEE = 'employee',
  ADMIN = 'admin'
}

export enum EmployeeRole {
  ADMIN = 'admin',
  MANAGER = 'manager',
  EMPLOYEE = 'employee',
}
